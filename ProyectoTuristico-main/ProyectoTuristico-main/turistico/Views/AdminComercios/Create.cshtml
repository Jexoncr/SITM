@model turistico.Controllers.ComercioAdminVM

@{
    ViewBag.Title = "Agregar Comercio";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
    <div>
        <h3 class="mb-1">Agregar comercio</h3>
        <div class="text-muted">Complete la información para registrar un comercio en el sistema.</div>
    </div>

    <div>
        <a class="btn btn-outline-secondary" href="@Url.Action("Index","AdminComercios")">Volver</a>
    </div>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body">
        @using (Html.BeginForm("Create", "AdminComercios", FormMethod.Post, new { autocomplete = "off" }))
        {
            @Html.AntiForgeryToken()

            @Html.ValidationSummary(true, "", new { @class = "alert alert-danger", role = "alert" })

            <div class="row g-3">
                <!-- Básicos -->
                <div class="col-12 col-md-6">
                    <label class="form-label">Nombre del comercio <span class="text-danger">*</span></label>
                    @Html.TextBoxFor(m => m.Nombre, new { @class = "form-control", placeholder = "Ej: Cafetería La Plaza", required = "required" })
                    @Html.ValidationMessageFor(m => m.Nombre, "", new { @class = "text-danger small" })
                </div>

                <div class="col-12 col-md-6">
                    <label class="form-label">Categoría <span class="text-danger">*</span></label>
                    @Html.DropDownListFor(m => m.CategoriaId, (SelectList)ViewBag.Categorias, "Seleccione...", new { @class = "form-select", required = "required" })
                    @Html.ValidationMessageFor(m => m.CategoriaId, "", new { @class = "text-danger small" })
                </div>

                <div class="col-12">
                    <label class="form-label">Descripción</label>
                    @Html.TextAreaFor(m => m.Descripcion, 3, 1, new { @class = "form-control", placeholder = "Descripción corta del comercio..." })
                    @Html.ValidationMessageFor(m => m.Descripcion, "", new { @class = "text-danger small" })
                </div>

                <!-- Ubicación / contacto -->
                <div class="col-12 col-md-6">
                    <label class="form-label">Dirección</label>
                    @Html.TextBoxFor(m => m.Direccion, new { @class = "form-control", placeholder = "Ej: San José, Desamparados..." })
                    @Html.ValidationMessageFor(m => m.Direccion, "", new { @class = "text-danger small" })
                </div>

                <div class="col-12 col-md-3">
                    <label class="form-label">Teléfono</label>
                    @Html.TextBoxFor(m => m.Telefono, new { @class = "form-control", placeholder = "Ej: 8888-8888" })
                    @Html.ValidationMessageFor(m => m.Telefono, "", new { @class = "text-danger small" })
                </div>

                <div class="col-12 col-md-3">
                    <label class="form-label">Horario</label>
                    @Html.TextBoxFor(m => m.Horario, new { @class = "form-control", placeholder = "Ej: Lun-Vie 8am-6pm" })
                    @Html.ValidationMessageFor(m => m.Horario, "", new { @class = "text-danger small" })
                </div>

                <div class="col-12 col-md-6">
                    <label class="form-label">Sitio web</label>
                    @Html.TextBoxFor(m => m.SitioWeb, new { @class = "form-control", placeholder = "https://..." })
                    @Html.ValidationMessageFor(m => m.SitioWeb, "", new { @class = "text-danger small" })
                </div>

                <div class="col-12 col-md-3">
                    <label class="form-label">Latitud</label>
                    @Html.TextBoxFor(m => m.Latitud, new { @class = "form-control", type = "number", step = "0.000001", placeholder = "Ej: 9.933333" })
                    @Html.ValidationMessageFor(m => m.Latitud, "", new { @class = "text-danger small" })
                </div>

                <div class="col-12 col-md-3">
                    <label class="form-label">Longitud</label>
                    @Html.TextBoxFor(m => m.Longitud, new { @class = "form-control", type = "number", step = "0.000001", placeholder = "Ej: -84.083333" })
                    @Html.ValidationMessageFor(m => m.Longitud, "", new { @class = "text-danger small" })
                </div>

                <!-- Estado -->
                <div class="col-12 col-md-4">
                    <label class="form-label">Estado</label>
                    @Html.DropDownListFor(m => m.Estado,
                        new SelectList(new[] { "Aprobado", "Pendiente", "Rechazado" }, Model?.Estado ?? "Aprobado"),
                        new { @class = "form-select" })
                    <div class="form-text">Recomendado: <b>Aprobado</b> si lo crea el Admin.</div>
                </div>

                <!-- Regulación -->
                <div class="col-12 col-md-8 d-flex align-items-end">
                    <div class="form-check">
                        @Html.CheckBoxFor(m => m.EsRegulado, new { @class = "form-check-input", id = "chkRegulado" })
                        <label class="form-check-label" for="chkRegulado">
                            Es comercio regulado (requiere patente)
                        </label>
                    </div>
                </div>

                <div class="col-12 col-md-6">
                    <label class="form-label">Número de patente</label>
                    @Html.TextBoxFor(m => m.NumeroPatente, new { @class = "form-control", id = "txtPatente", placeholder = "Ej: PAT-2026-001" })
                    @Html.ValidationMessageFor(m => m.NumeroPatente, "", new { @class = "text-danger small" })
                </div>

                <div class="col-12 col-md-6">
                    <label class="form-label">Fecha de vencimiento</label>
                    @Html.TextBoxFor(m => m.FechaVencimiento, new { @class = "form-control", type = "date", id = "txtVence" })
                    @Html.ValidationMessageFor(m => m.FechaVencimiento, "", new { @class = "text-danger small" })
                </div>
            </div>

            <hr class="my-4" />

            <div class="d-flex flex-wrap gap-2">
                <button class="btn btn-success" type="submit">Guardar</button>
                <a class="btn btn-outline-secondary" href="@Url.Action("Index","AdminComercios")">Cancelar</a>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        (function () {
            const chk = document.getElementById('chkRegulado');
            const patente = document.getElementById('txtPatente');
            const vence = document.getElementById('txtVence');

            function toggleRegulado() {
                const on = chk && chk.checked;
                if (patente) patente.disabled = !on;
                if (vence) vence.disabled = !on;

                // Limpieza opcional al desactivar
                if (!on) {
                    if (patente) patente.value = "";
                    if (vence) vence.value = "";
                }
            }

            if (chk) chk.addEventListener('change', toggleRegulado);
            toggleRegulado();
        })();
    </script>
}
